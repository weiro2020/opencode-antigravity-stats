#!/bin/bash
# Wrapper script for get_antigravity_quota.py
# Called by the plugin to fetch quota data

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
python3 "$SCRIPT_DIR/get_antigravity_quota.py" "$@"
